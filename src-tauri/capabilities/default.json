{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for CopyTranslator settings and translation windows",
  "windows": ["main", "translation"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
